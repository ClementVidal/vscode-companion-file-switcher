{
    "name": "file-switcher",
    "displayName": "file-switcher",
    "description": "Switch accross file based on their extensions",
    "version": "0.0.1",
    "publisher": "ClementVidal",
    "engines": {
        "vscode": "^1.0.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.fileSwitcher.switch"
    ],
    "main": "./out/src/extension",
    "contributes": {
        "commands": [{
            "command": "extension.fileSwitcher.switch",
            "title": "Switch"
        }]
    },
    "scripts": {
        "vscode:prepublish": "node ./node_modules/vscode/bin/compile",
        "compile": "node ./node_modules/vscode/bin/compile -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install"
    },
    "devDependencies": {
        "typescript": "^1.8.5",
        "vscode": "^0.11.0"
    }
}